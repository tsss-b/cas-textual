# castcode

A terminal UI for the Claude Agent SDK, built with Textual.

## Install

From a release branch checkout or downloaded ZIP:

```sh
uv tool install .
castcode
```

`castcode` uses the directory where you launch it as the agent working directory.

