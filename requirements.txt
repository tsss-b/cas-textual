# Python 3.12. Reproduce the dev env with:
#   python3.12 -m venv .venv && .venv/bin/pip install -r requirements.txt
# Always run tools via .venv/bin/... (see CLAUDE.md / HANDOFF.md).
textual==8.2.7
textual-dev==1.8.0
claude-agent-sdk==0.2.110
pytest==9.1.1
pytest-asyncio==1.4.0
